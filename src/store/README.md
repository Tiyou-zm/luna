# Store

This folder is reserved for Zustand stores used across pages.

Keep stores small and feature-scoped. Export hooks from each store file so pages can consume state without coupling to store internals.
